# Set C protected-span check and the L6 options, 2026-08-16

Raw data: `raw/setC-cleanup-input-2026-08-16.jsonl` (the 30 Set C recognizer
outputs from `~/Desktop/chalant-corpus/manifest.en_US.jsonl`, 08-15) and
`raw/setC-cleanup-run{1,2,3}-2026-08-16.jsonl` (the shipping cleanup path,
`tools/shipclean --fresh`, three runs). L6 numbers come from the day's live
dictation log (48 utterances with an insertion, 16:15 to 19:45, 1.15.1 to
1.16.0 as installed).

## Protected-span mutation rate on Set C, as STAGE1_DIRECTIVE.md §4.1 defines it

**Not computable.** Set C exists (C01-C30, ground truth and audio present,
categories match §2.1) but no utterance carries the `protected_spans` list the
metric counts against (§2.3). Nothing was invented in its place.

**Upper bound, measured:** utterances the cleanup layer changed at all.

| run | changed | shipped raw by the guard |
|---|---|---|
| 1 | 4/30 | 1 (C12: the model replied) |
| 2 | 5/30 | 1 |
| 3 | 4/30 | 1 |

Every change across the 90 utterance-runs: C14 gained a comma ("153, not
135"); C18, C23, C27 lost a doubled opening ("The the", "Reply to reply to");
C30 in run 2 gained an appended stray line "TRANSCRIPT." (a fragment of the
framing marker `unwrap` does not strip; a defect, not fixed). **No numeral,
negation, name, environment, path, time or destructive verb changed.**
Recognizer errors upstream of cleanup, unchanged by it: "315" for "3:15",
"bill" for "build", "ABI" for "API", "/user/Chatan" for the path.

## L6, three options, from the same day's 47 paired live dictations

| option | L6 median | L6 p95 | Set C exposure (utterances the cleanup touches, per run) | output-quality cost on 92 real utterances |
|---|---|---|---|---|
| A. cleanup off the critical path (no swap-in later; that is banned, so: off) | **0.21 s** | 0.64 s | 0/30 | loses all 22 model changes: fillers stay 14 (model took them to 7), model-removed stutters stay |
| B. cleanup only above 40 chars | **0.35 s** | 1.87 s | 2/30 (C23, C27, both stutter removals; C30's leak case not cleaned) | loses 2 of 22 changes, both trivial ("How do we uh?" -> "How do we?", a trailing false start); fillers 13->7 instead of 14->7; 43% of utterances still cleaned |
| C. keep as is, change the target | 0.86 s | 2.11 s | 4-5/30, 0 protected categories, 1 stray-token leak in 90 | none |

Other thresholds measured: >30 chars median 0.69 s (55% cleaned); >60 chars
0.22 s (17% cleaned, loses 4 of 22 changes and 3 filler removals).
