
## Decision and implementation (founder: "go B", 2026-08-16 ~20:05)

`CleanupPrompt.worthCleaning`: the model runs only above 40 characters
(`minimumCharactersForCleanup`); deterministic passes still run on everything.
Polisher and `tools/shipclean` both consult it (`shipclean --all` ignores it, to
measure what the model would do to everything).

Measured with the gate, shipping Core:

| set | skipped by the gate | changed | rejected |
|---|---|---|---|
| 92 real utterances | 44/92 | 18/92 (of the 48 long ones) | 2 |
| Set C | 19/30 | 3/30: C23, C27 (doubled openings removed), C30 (**"TRANSCRIPT." leak again**, 43 chars so it is above the line) | 0 |

**Open defect, not fixed here:** C30 has now leaked the stray line
"TRANSCRIPT." in 2 of 4 runs. `CleanupPrompt.unwrap` strips the full closing
marker `TRANSCRIPT>>>` but not a bare `TRANSCRIPT` / `TRANSCRIPT.` fragment the
model sometimes appends. It is corruption of the user's text (an added token,
which no guard rule covers) and it predates option B. One-line fix candidate,
awaiting a go.

Live L6 on the installed build: to be read from the founder's next dictations.
